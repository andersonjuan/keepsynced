use rusoto_core::Region;
use configs::Configs;

mod s3 {
    struct AWS {
        region: str,
        bucket: str,
    }

    impl Aws {
        pub fn upload() -> Result {
            
        }
    }
}
